function loadTracks(filename) {
    if (window.File && window.FileReader && window.FileList && window.Blob) {
        // time to parse shit
    }
    else {
        alert('Your browser does not support the File API, and is not compatible with FFSTP.');
    }
}
